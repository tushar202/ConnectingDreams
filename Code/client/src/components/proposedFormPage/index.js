import React from "react";
import { Container } from "react-bootstrap";

import Form from "./Form";

const CMFormPage = () => {
  return (
    <Container>
      <Form />
    </Container>
  );
};

export default CMFormPage;